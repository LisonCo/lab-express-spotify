<div id="albums-container">
    {{#albums}}
    {{#body}}
    {{#each items}}   
    <div id="album-card">
        {{ name }}
        <img src="{{images.[0].url}}">
        <a href="/albums/tracks/{{ id }}">View Tracks</a>
    </div>
    {{/each}}
    {{/body}}
    {{/albums}}
</div>