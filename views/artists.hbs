<div id="artists-container">
    {{#artists}}
    {{#each items}}   

    <div id="artist-card">
        {{ name }}
        <img src="{{images.[0].url}}">
        <a href="/albums/{{ id }}">View Albums</a>
    </div>
    {{/each}}
    {{/artists}}
</div>